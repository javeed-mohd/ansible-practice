- name: Check a number less than 10
  hosts: local
  connection: local
  vars:
    NUMBER_OF_SESSIONS: 7
  tasks:
  - name: Is ansible more than 5 sessions
    ansible.builtin.debug:
      msg: "Yes, Ansible is more than 5 sessions"
    when: NUMBER_OF_SESSIONS > 8