- name: Variable preference
  hosts: local
  connection: local
  # vars:
  #   NAME: "Ansible from PLAY"
  # vars_files:
  # - course.yaml
  # vars_prompt:
  # - name: NAME
  #   prompt: Please enter your name
  #   private: false
  tasks:
  - name: Print name
    # vars:
    #   NAME: "Ansible from TASK"
    ansible.builtin.debug:
      msg: "Hi {{ NAME}}"


# 1. Args
# 2. Task (So commented)
# 3. File (So commented)
# 4. Prompt (So commented)
# 5. Play (So commented)
# 6. Inventory (I not any of these, then we will get undefined variable as output in terminal)